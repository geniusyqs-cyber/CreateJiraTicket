User Story Name: As data engineer, I need to troubleshoot the empty metric value error during Panaray file generation, identify whether the blank value comes from abnormal source data or code logic defect, then implement targeted fix to resolve database write failure.
Deliverable: Root cause confirmed (source data issue / code parsing bug), fixed code deployed, Panaray CSV file generation and PostgreSQL copy write process run successfully without integer conversion error.
1. Context and Background
The Panaray data file generation task failed during database writing on 2026-06-25. The error log shows psycopg2.errors.InvalidTextRepresentation: invalid input syntax for type integer: "".
Root trigger: A single metric field is empty string "", while the target database column is defined as integer type; empty string cannot be cast to integer, leading to copy_from write failure in hk_mapping3.py.
We need to first verify business rules: whether this metric is allowed to be empty from upstream source data, then decide to either fix abnormal raw source data or add empty value compatible logic in parsing code.

2. Detail Requirement
Reproduce the failed task based on log traceback:
Script path: panaray_data/csv_smb/hk_mapping3.py
Crash line: Line 1485 psql_insert_copy function calls cur.copy_from to write temporary table
Error reason: Empty string value inserted into integer column of tmp_table
Locate which specific metric column appears empty string in the generated CSV data.
Check upstream raw data source: Confirm if the metric is supposed to have empty value per business definition.
Classify root cause and implement corresponding fix:
Case 1: Source data abnormal (empty value not allowed): Clean up upstream invalid source data.
Case 2: Code parsing defect (lack empty value conversion): Revise parsing logic to convert empty string to NULL/valid integer placeholder before database write.
Re-run the full hk_mapping3.py update workflow, verify no type conversion error occurs, Panaray data file is generated and imported to database completely.
Record root cause analysis and fix solution in ticket for future reference.

3. Actual Expectation
The specific empty metric field and root cause (source data / code defect) are clearly identified and documented.
Corresponding repair work is completed (source data cleaning or code logic adjustment).
Panaray data file generation task runs successfully without invalid input syntax for type integer error.
Complete data can be written to PostgreSQL temporary table and formal business table normally.